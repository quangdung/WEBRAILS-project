<%= form_for(@espece) do |f| %>
  <% if @espece.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@espece.errors.count, "error") %> prohibited this espece from being saved:</h2>

      <ul>
      <% @espece.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
