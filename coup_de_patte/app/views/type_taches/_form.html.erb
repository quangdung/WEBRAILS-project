<%= form_for(@type_tach) do |f| %>
  <% if @type_tach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_tach.errors.count, "error") %> prohibited this type_tach from being saved:</h2>

      <ul>
      <% @type_tach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-sm btn-default" %>
  </div>
<% end %>
