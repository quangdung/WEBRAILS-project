<% content_for :actions_specifiques do %>
    <%= render :partial => "actions_specifiques", :locals => {:action => "index" } %>
<% end %>
<!--<p id="notice"><%= notice %></p>-->

<h1>Liste des especes</h1>

<table class="table table-striped table-hover table-bordered table-responsive">
  <thead>
    <tr>
      <th>Nom</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @especes.each do |espece| %>
      <tr>
        <td><%= espece.nom %></td>
        <td>
          <%= link_to 'Afficher', espece, :class => 'btn btn-sm btn-default' %>
          <%= link_to 'Modifier', edit_espece_path(espece), :class => 'btn btn-sm btn-default' %>
          <%= link_to 'Détruire', espece, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-sm btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvelle espece', new_espece_path %>
